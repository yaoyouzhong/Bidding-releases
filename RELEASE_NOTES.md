# 标书工作台 0.3.0

本版纳入材料识别、浏览器查询、解析更新及应用内自动更新。

- 启动后自动检查新版本，查看说明并确认后可一键升级；校验更新签名，保留备份并在启动失败时恢复旧版。普通用户使用 EXE 安装包的默认安装位置即可启用。
- 改进人员证件分类和本地 OCR 复核，减少把包含身份证号的学历证书误归为身份证的情况。
- 改进混合 PDF/DOCX 的文字和图片识别，默认采用 PDFium。
- 大批 OCR 按实际待识别页数和本机资源按需校准并发；保留取消、失败回退及缓存机制。
- 报价初步核对和评分候选证据接入现有响应核对，保留来源与人工确认，不自动认定得分。
- 企业信用和学历查询采用专用 Chrome 会话，保留验证码自动处理、人工接手和截图约束。
- 随安装包和更新包提供验证码模型、第三方许可证及第三方源码资料；支持按说明使用兼容修改版 GEOS。

机器级 MSI 安装及数据库结构变化的版本使用完整安装包升级。请保留重要资料备份；机器检查结果不替代人工核对和终审。
