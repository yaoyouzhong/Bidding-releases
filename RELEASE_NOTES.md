# 标书工作台 0.4.2

本版改为精简 EXE 安装包与独立模型包，安装包约 116.14 MiB。已有匹配模型可继续复用，程序升级无需重复下载模型。

## 更新内容
- 模型导入支持 ZIP 包和 ONNX 文件，可同时选择多个文件；增加缺模型、部分就绪和实际保存目录提示。
- 模型独立保存在当前用户目录，更换程序安装位置不影响缓存复用；模型更新保留旧版本。
- 人员证件支持单字段复核，人员列表按需加载图片；企业证书支持更新并保留旧原件和历史记录。
- 简化应用设置中的重复信息，优化操作人署名及来源页码布局。
- 新版使用同一个 EXE 安装包进行手动安装和后续应用内更新，保留签名校验、任务保护与失败恢复机制。

## 安装与升级
- 0.4.1 及以前版本：请下载本页 EXE，保存工作并退出旧程序后手动安装一次。旧 ZIP 更新通道保持原兼容版本，不会自动升级至 0.4.2。
- 新安装：在“应用设置 → 本地识别模型 → 导入模型”选择本页文档模型包、验证码模型包，无需解压；也可使用“下载模型”。已有模型显示就绪时无需重新导入。
- 只提供 Windows 10/11 x64 EXE，不提供本版 MSI。默认当前用户安装；自选位置超出应用内更新支持范围时，使用安装包手动升级。
- 模型导入后可离线识别。新电脑若没有 WebView2，安装引导仍需要联网安装该运行库。

## 验证范围
本版已完成打包后端的离线模型导入及实际推理、重启持久化、旧库升级与异常数据库保护，以及安装包内容、生产前端内嵌和静态运行库核对。本次精简包未重新执行干净机安装/卸载和完整签名升级回滚验收，不把此前完整包的验收结果当作本包实测。

程序包和更新仅通过 GitHub 分发。源码保持私有；仅供学习交流，不用于商业用途。第三方组件按各自许可证提供，详见 third-party.zip；安装包未取得 Windows Authenticode 签名，更新签名与文件 SHA-256 校验另行提供。
